// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// Settings required by Feeds of Custom API (Codeless).
/// Supports fetching data from third-party APIs with configurable pagination,
/// authentication, and response parsing.
public struct CustomAPISettings: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Required. Enter the primary web address of the third-party API (e.g.,
  /// https://api.vendor.com).
  public var baseUrl: Swift.String = Swift.String()

  /// Optional. Specify how often the platform checks the API for new data in
  /// minutes (default is 15 minutes).
  public var pollingFrequency: Swift.Int32 = Swift.Int32()

  /// Required. Configure the initial API call used to fetch the main list of
  /// logs or events.
  public var primaryRequest: CustomAPITransferNode? = nil

  /// Choose the method and credentials used to securely authorize access to the
  /// API.
  public var authentication: OneOf_Authentication? = nil

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `CustomAPISettings`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = CustomAPISettings().with { $0.noAuth = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let noAuth = CodingKeys(stringValue: "noAuth")
    static let basicAuth = CodingKeys(stringValue: "basicAuth")
    static let oauthClientCredentials = CodingKeys(stringValue: "oauthClientCredentials")
    static let headerAuth = CodingKeys(stringValue: "headerAuth")
    static let queryAuth = CodingKeys(stringValue: "queryAuth")
    static let baseUrl = CodingKeys(stringValue: "baseUrl")
    static let pollingFrequency = CodingKeys(stringValue: "pollingFrequency")
    static let primaryRequest = CodingKeys(stringValue: "primaryRequest")

    static let _knownKeys: Set<Swift.String> = [
      "noAuth",
      "basicAuth",
      "oauthClientCredentials",
      "headerAuth",
      "queryAuth",
      "baseUrl",
      "pollingFrequency",
      "primaryRequest",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .baseUrl) {
      self.baseUrl = value
    }
    if let value = try container.decodeIfPresent(Swift.Int32.self, forKey: .pollingFrequency) {
      self.pollingFrequency = value
    }
    self.primaryRequest = try container.decodeIfPresent(
      CustomAPITransferNode.self, forKey: .primaryRequest)

    var authentication: OneOf_Authentication? = nil
    let authenticationCheckAndSet = {
      if authentication != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'authentication'"))
      }
      authentication = $0
    }
    if let noAuth = try container.decodeIfPresent(CustomAPINoAuth?.self, forKey: .noAuth) {
      try authenticationCheckAndSet(.noAuth(noAuth))
    }
    if let basicAuth = try container.decodeIfPresent(UsernameSecretAuth?.self, forKey: .basicAuth) {
      try authenticationCheckAndSet(.basicAuth(basicAuth))
    }
    if let oauthClientCredentials = try container.decodeIfPresent(
      OAuthClientCredentials?.self, forKey: .oauthClientCredentials)
    {
      try authenticationCheckAndSet(.oauthClientCredentials(oauthClientCredentials))
    }
    if let headerAuth = try container.decodeIfPresent(
      CustomAPIHeaderAuth?.self, forKey: .headerAuth)
    {
      try authenticationCheckAndSet(.headerAuth(headerAuth))
    }
    if let queryAuth = try container.decodeIfPresent(CustomAPIQueryAuth?.self, forKey: .queryAuth) {
      try authenticationCheckAndSet(.queryAuth(queryAuth))
    }
    self.authentication = authentication
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.baseUrl, forKey: .baseUrl)
    try container.encode(self.pollingFrequency, forKey: .pollingFrequency)
    try container.encodeIfPresent(self.primaryRequest, forKey: .primaryRequest)

    if let choice = self.authentication {
      switch choice {
      case .noAuth(let value):
        try container.encode(value, forKey: .noAuth)
      case .basicAuth(let value):
        try container.encode(value, forKey: .basicAuth)
      case .oauthClientCredentials(let value):
        try container.encode(value, forKey: .oauthClientCredentials)
      case .headerAuth(let value):
        try container.encode(value, forKey: .headerAuth)
      case .queryAuth(let value):
        try container.encode(value, forKey: .queryAuth)
      }
    }
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Choose the method and credentials used to securely authorize access to the
  /// API.
  public enum OneOf_Authentication: Codable, Equatable, Sendable {
    /// Authenticate without credentials or secrets.
    indirect case noAuth(CustomAPINoAuth?)
    /// Authenticate using Username/Password credentials.
    indirect case basicAuth(UsernameSecretAuth?)
    /// Authenticate using OAuth 2.0 Client Credentials Grant flow.
    indirect case oauthClientCredentials(OAuthClientCredentials?)
    /// Authenticate using custom API Keys injected into request headers.
    indirect case headerAuth(CustomAPIHeaderAuth?)
    /// Authenticate using custom API Keys injected into URL query parameters.
    indirect case queryAuth(CustomAPIQueryAuth?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.chronicle.v1.CustomAPISettings"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
