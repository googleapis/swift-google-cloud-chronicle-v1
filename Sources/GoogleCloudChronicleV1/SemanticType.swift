// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation

/// An enumeration of the possible types of a `Feed.details` field, where type
/// implies both encoding and semantics. This is used in constructing a schema in
/// order to construct a UI for creating well formed feeds.
public enum SemanticType: Codable, Equatable, Sendable {
  /// No semantic type. All fields must specify a semantic type.
  case unspecified
  /// A boolean (with no special semantics).
  case bool
  /// An enum (with no special semantics).
  case `enum`
  /// A repeated field with a Message type where the Message contains two
  /// fields: `key` and `value`.
  case keyValueList
  /// A `map<string, string>` (with no special semantics).
  case mapStringString
  /// A string (with no special semantics).
  case string
  /// A repeated string (with no special semantics).
  case stringList
  /// A string that may contain any whitespace characters (with no special
  /// semantics).
  case stringMultiline
  /// A string that may contain any whitespace characters and that encodes a
  /// secret.
  case stringMultilineSecret
  /// A string which encodes a secret.
  case stringSecret
  /// A string which encodes a URI.
  case stringUri
  /// A repeated string which encodes URIs.
  case stringUriList
  /// Encodes an unknown integer value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownIntValue(Int)
  /// Encodes an unknown string value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownStringValue(String)

  public init() {
    self = .unspecified
  }

  /// Returns the integer value associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown string value, this returns `nil`.
  public var intValue: Int? {
    switch self {
    case .unspecified: return 0
    case .bool: return 1
    case .`enum`: return 2
    case .keyValueList: return 3
    case .mapStringString: return 4
    case .string: return 5
    case .stringList: return 6
    case .stringMultiline: return 7
    case .stringMultilineSecret: return 8
    case .stringSecret: return 9
    case .stringUri: return 10
    case .stringUriList: return 11
    case .unknownIntValue(let v): return v
    case .unknownStringValue: return nil
    }
  }

  /// Returns the string value (or name) associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
  public var stringValue: Swift.String? {
    switch self {
    case .unspecified: return "SEMANTIC_TYPE_UNSPECIFIED"
    case .bool: return "BOOL"
    case .`enum`: return "ENUM"
    case .keyValueList: return "KEY_VALUE_LIST"
    case .mapStringString: return "MAP_STRING_STRING"
    case .string: return "STRING"
    case .stringList: return "STRING_LIST"
    case .stringMultiline: return "STRING_MULTILINE"
    case .stringMultilineSecret: return "STRING_MULTILINE_SECRET"
    case .stringSecret: return "STRING_SECRET"
    case .stringUri: return "STRING_URI"
    case .stringUriList: return "STRING_URI_LIST"
    case .unknownIntValue: return nil
    case .unknownStringValue(let v): return v
    }
  }

  /// Initialize from a string value.
  ///
  /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SemanticType/unknownStringValue(_:)).
  public init(stringValue: Swift.String) {
    switch stringValue {
    case "SEMANTIC_TYPE_UNSPECIFIED": self = .unspecified
    case "BOOL": self = .bool
    case "ENUM": self = .`enum`
    case "KEY_VALUE_LIST": self = .keyValueList
    case "MAP_STRING_STRING": self = .mapStringString
    case "STRING": self = .string
    case "STRING_LIST": self = .stringList
    case "STRING_MULTILINE": self = .stringMultiline
    case "STRING_MULTILINE_SECRET": self = .stringMultilineSecret
    case "STRING_SECRET": self = .stringSecret
    case "STRING_URI": self = .stringUri
    case "STRING_URI_LIST": self = .stringUriList
    default: self = .unknownStringValue(stringValue)
    }
  }

  /// Initialize from an integer value.
  ///
  /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SemanticType/unknownIntValue(_:)).
  public init(intValue: Int) {
    switch intValue {
    case 0: self = .unspecified
    case 1: self = .bool
    case 2: self = .`enum`
    case 3: self = .keyValueList
    case 4: self = .mapStringString
    case 5: self = .string
    case 6: self = .stringList
    case 7: self = .stringMultiline
    case 8: self = .stringMultilineSecret
    case 9: self = .stringSecret
    case 10: self = .stringUri
    case 11: self = .stringUriList
    default: self = .unknownIntValue(intValue)
    }
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.singleValueContainer()
    if let v = try? container.decode(Int.self) {
      self.init(intValue: v)
      return
    }
    if let s = try? container.decode(String.self) {
      if let v = Int(s) {
        self.init(intValue: v)
      } else {
        self.init(stringValue: s)
      }
      return
    }
    throw DecodingError.dataCorruptedError(
      in: container, debugDescription: "Expected enum value, must be integer or string.")
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.singleValueContainer()
    switch self {
    case .unspecified: return try container.encode("SEMANTIC_TYPE_UNSPECIFIED")
    case .bool: return try container.encode("BOOL")
    case .`enum`: return try container.encode("ENUM")
    case .keyValueList: return try container.encode("KEY_VALUE_LIST")
    case .mapStringString: return try container.encode("MAP_STRING_STRING")
    case .string: return try container.encode("STRING")
    case .stringList: return try container.encode("STRING_LIST")
    case .stringMultiline: return try container.encode("STRING_MULTILINE")
    case .stringMultilineSecret: return try container.encode("STRING_MULTILINE_SECRET")
    case .stringSecret: return try container.encode("STRING_SECRET")
    case .stringUri: return try container.encode("STRING_URI")
    case .stringUriList: return try container.encode("STRING_URI_LIST")
    case .unknownIntValue(let v): return try container.encode(v)
    case .unknownStringValue(let v): return try container.encode(v)
    }
  }
}
