// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// Checkpoint configuration to enable sequential (cursor-based) polling.
public struct CustomAPICheckpointConfig: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Select the specific checkpointing mechanism the third-party API requires.
  public var strategy: OneOf_Strategy? = nil

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `CustomAPICheckpointConfig`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = CustomAPICheckpointConfig().with { $0.noneStrategy = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let noneStrategy = CodingKeys(stringValue: "noneStrategy")
    static let latestTimestampStrategy = CodingKeys(stringValue: "latestTimestampStrategy")
    static let latestRecordStrategy = CodingKeys(stringValue: "latestRecordStrategy")
    static let iteratorStrategy = CodingKeys(stringValue: "iteratorStrategy")

    static let _knownKeys: Set<Swift.String> = [
      "noneStrategy",
      "latestTimestampStrategy",
      "latestRecordStrategy",
      "iteratorStrategy",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)

    var strategy: OneOf_Strategy? = nil
    let strategyCheckAndSet = {
      if strategy != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'strategy'"))
      }
      strategy = $0
    }
    if let noneStrategy = try container.decodeIfPresent(
      CustomAPICheckpointConfig.NoneStrategy?.self, forKey: .noneStrategy)
    {
      try strategyCheckAndSet(.noneStrategy(noneStrategy))
    }
    if let latestTimestampStrategy = try container.decodeIfPresent(
      CustomAPICheckpointConfig.LatestTimestampStrategy?.self, forKey: .latestTimestampStrategy)
    {
      try strategyCheckAndSet(.latestTimestampStrategy(latestTimestampStrategy))
    }
    if let latestRecordStrategy = try container.decodeIfPresent(
      CustomAPICheckpointConfig.LatestRecordStrategy?.self, forKey: .latestRecordStrategy)
    {
      try strategyCheckAndSet(.latestRecordStrategy(latestRecordStrategy))
    }
    if let iteratorStrategy = try container.decodeIfPresent(
      CustomAPICheckpointConfig.IteratorStrategy?.self, forKey: .iteratorStrategy)
    {
      try strategyCheckAndSet(.iteratorStrategy(iteratorStrategy))
    }
    self.strategy = strategy
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)

    if let choice = self.strategy {
      switch choice {
      case .noneStrategy(let value):
        try container.encode(value, forKey: .noneStrategy)
      case .latestTimestampStrategy(let value):
        try container.encode(value, forKey: .latestTimestampStrategy)
      case .latestRecordStrategy(let value):
        try container.encode(value, forKey: .latestRecordStrategy)
      case .iteratorStrategy(let value):
        try container.encode(value, forKey: .iteratorStrategy)
      }
    }
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Fetch all available data in one go without tracking progress.
  public struct NoneStrategy: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `NoneStrategy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = NoneStrategy().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let _knownKeys: Set<Swift.String> = []
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.chronicle.v1.CustomAPICheckpointConfig.NoneStrategy"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Track the timestamp of the newest record to fetch newer ones next.
  public struct LatestTimestampStrategy: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Required. Enter the exact location within a log record where the
    /// checkpoint value (like a timestamp or ID) is found.
    public var checkpointValuePath: Swift.String = Swift.String()

    /// Required. Specify a custom name you assign to store and reference the
    /// checkpoint value between polling cycles.
    public var checkpointVariable: Swift.String = Swift.String()

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `LatestTimestampStrategy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = LatestTimestampStrategy().with { $0.checkpointValuePath = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let checkpointValuePath = CodingKeys(stringValue: "checkpointValuePath")
      static let checkpointVariable = CodingKeys(stringValue: "checkpointVariable")

      static let _knownKeys: Set<Swift.String> = [
        "checkpointValuePath",
        "checkpointVariable",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .checkpointValuePath)
      {
        self.checkpointValuePath = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .checkpointVariable) {
        self.checkpointVariable = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.checkpointValuePath, forKey: .checkpointValuePath)
      try container.encode(self.checkpointVariable, forKey: .checkpointVariable)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.chronicle.v1.CustomAPICheckpointConfig.LatestTimestampStrategy"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Track the highest record ID to fetch only new records next.
  public struct LatestRecordStrategy: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Required. Enter the exact location within a log record where the
    /// checkpoint value (like a timestamp or ID) is found.
    public var checkpointValuePath: Swift.String = Swift.String()

    /// Required. Specify a custom name you assign to store and reference the
    /// checkpoint value between polling cycles.
    public var checkpointVariable: Swift.String = Swift.String()

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `LatestRecordStrategy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = LatestRecordStrategy().with { $0.checkpointValuePath = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let checkpointValuePath = CodingKeys(stringValue: "checkpointValuePath")
      static let checkpointVariable = CodingKeys(stringValue: "checkpointVariable")

      static let _knownKeys: Set<Swift.String> = [
        "checkpointValuePath",
        "checkpointVariable",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .checkpointValuePath)
      {
        self.checkpointValuePath = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .checkpointVariable) {
        self.checkpointVariable = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.checkpointValuePath, forKey: .checkpointValuePath)
      try container.encode(self.checkpointVariable, forKey: .checkpointVariable)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.chronicle.v1.CustomAPICheckpointConfig.LatestRecordStrategy"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Use progress tokens provided by the API.
  public struct IteratorStrategy: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// Required. Enter the exact location within a log record where the
    /// checkpoint value (like a timestamp or ID) is found.
    public var checkpointValuePath: Swift.String = Swift.String()

    /// Required. Specify a custom name you assign to store and reference the
    /// checkpoint value between polling cycles.
    public var checkpointVariable: Swift.String = Swift.String()

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `IteratorStrategy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = IteratorStrategy().with { $0.checkpointValuePath = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let checkpointValuePath = CodingKeys(stringValue: "checkpointValuePath")
      static let checkpointVariable = CodingKeys(stringValue: "checkpointVariable")

      static let _knownKeys: Set<Swift.String> = [
        "checkpointValuePath",
        "checkpointVariable",
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .checkpointValuePath)
      {
        self.checkpointValuePath = value
      }
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .checkpointVariable) {
        self.checkpointVariable = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.checkpointValuePath, forKey: .checkpointValuePath)
      try container.encode(self.checkpointVariable, forKey: .checkpointVariable)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.chronicle.v1.CustomAPICheckpointConfig.IteratorStrategy"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Select the specific checkpointing mechanism the third-party API requires.
  public enum OneOf_Strategy: Codable, Equatable, Sendable {
    /// Fetch all available data in one go without tracking progress.
    indirect case noneStrategy(CustomAPICheckpointConfig.NoneStrategy?)
    /// Track the timestamp of the newest record to fetch newer ones next.
    indirect case latestTimestampStrategy(CustomAPICheckpointConfig.LatestTimestampStrategy?)
    /// Track the highest record ID to fetch only new records next.
    indirect case latestRecordStrategy(CustomAPICheckpointConfig.LatestRecordStrategy?)
    /// Use progress tokens provided by the API.
    indirect case iteratorStrategy(CustomAPICheckpointConfig.IteratorStrategy?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.chronicle.v1.CustomAPICheckpointConfig"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
