// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// CrowdStrike Alerts settings.
public struct CrowdStrikeAlertsSettings: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Required. OAuthClientCredentials.
  public var authentication: OAuthClientCredentials? = nil

  /// Required. API Hostname.
  public var hostname: Swift.String = Swift.String()

  /// Optional. Ingestion Type.
  public var ingestionType: CrowdStrikeAlertsSettings.IngestionType =
    CrowdStrikeAlertsSettings.IngestionType()

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `CrowdStrikeAlertsSettings`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = CrowdStrikeAlertsSettings().with { $0.authentication = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let authentication = CodingKeys(stringValue: "authentication")
    static let hostname = CodingKeys(stringValue: "hostname")
    static let ingestionType = CodingKeys(stringValue: "ingestionType")

    static let _knownKeys: Set<Swift.String> = [
      "authentication",
      "hostname",
      "ingestionType",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.authentication = try container.decodeIfPresent(
      OAuthClientCredentials.self, forKey: .authentication)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .hostname) {
      self.hostname = value
    }
    if let value = try container.decodeIfPresent(
      CrowdStrikeAlertsSettings.IngestionType.self, forKey: .ingestionType)
    {
      self.ingestionType = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encodeIfPresent(self.authentication, forKey: .authentication)
    try container.encode(self.hostname, forKey: .hostname)
    try container.encode(self.ingestionType, forKey: .ingestionType)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Ingestion Type.
  public enum IngestionType: Codable, Equatable, Sendable {
    /// For the feeds in which this field is not set
    case unspecified
    /// Ingests both new as well as old alerts which are updated
    case bringAllAlerts
    /// Ingests only new alerts to be ingested.
    case bringOnlyNewAlerts
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .bringAllAlerts: return 1
      case .bringOnlyNewAlerts: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "INGESTION_TYPE_UNSPECIFIED"
      case .bringAllAlerts: return "BRING_ALL_ALERTS"
      case .bringOnlyNewAlerts: return "BRING_ONLY_NEW_ALERTS"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:IngestionType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "INGESTION_TYPE_UNSPECIFIED": self = .unspecified
      case "BRING_ALL_ALERTS": self = .bringAllAlerts
      case "BRING_ONLY_NEW_ALERTS": self = .bringOnlyNewAlerts
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:IngestionType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .bringAllAlerts
      case 2: self = .bringOnlyNewAlerts
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("INGESTION_TYPE_UNSPECIFIED")
      case .bringAllAlerts: return try container.encode("BRING_ALL_ALERTS")
      case .bringOnlyNewAlerts: return try container.encode("BRING_ONLY_NEW_ALERTS")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.chronicle.v1.CrowdStrikeAlertsSettings"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
