// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// FeedPack is a logical container for related LogTypes for which feeds can be
/// configured.
public struct FeedPack: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Identifier. The resource name of the feed pack.
  /// Format:
  /// projects/{project}/locations/{location}/instances/{instance}/feedPacks/{feed_pack}
  public var name: Swift.String = Swift.String()

  /// Output only. The display name of the feed pack.
  public var displayName: Swift.String = Swift.String()

  /// Output only. The description of the feed pack.
  public var description: Swift.String = Swift.String()

  /// Output only. The icon image of the content pack in base64 in png format.
  public var icon: Foundation.Data = Foundation.Data()

  /// Output only. Categories the featured content is associated with. In case of
  /// product feed packs, there will be only one category.
  public var categories: [Swift.String] = []

  /// Output only. Type of pack.
  public var packType: FeedPack.PackType = FeedPack.PackType()

  /// Output only. Log types featured in the pack.
  public var packLogTypes: [PackLogType] = []

  /// Output only. Whether the feed pack should be displayed in the Feeds Page.
  public var hidden: Swift.Bool = Swift.Bool()

  /// Output only. Pack specific documentation in markdown format.
  public var packDocumentation: Swift.String = Swift.String()

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `FeedPack`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = FeedPack().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let name = CodingKeys(stringValue: "name")
    static let displayName = CodingKeys(stringValue: "displayName")
    static let description = CodingKeys(stringValue: "description")
    static let icon = CodingKeys(stringValue: "icon")
    static let categories = CodingKeys(stringValue: "categories")
    static let packType = CodingKeys(stringValue: "packType")
    static let packLogTypes = CodingKeys(stringValue: "packLogTypes")
    static let hidden = CodingKeys(stringValue: "hidden")
    static let packDocumentation = CodingKeys(stringValue: "packDocumentation")

    static let _knownKeys: Set<Swift.String> = [
      "name",
      "displayName",
      "description",
      "icon",
      "categories",
      "packType",
      "packLogTypes",
      "hidden",
      "packDocumentation",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
      self.name = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .displayName) {
      self.displayName = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .description) {
      self.description = value
    }
    if let value = try container.decodeIfPresent(Foundation.Data.self, forKey: .icon) {
      self.icon = value
    }
    if let value = try container.decodeIfPresent([Swift.String].self, forKey: .categories) {
      self.categories = value
    }
    if let value = try container.decodeIfPresent(FeedPack.PackType.self, forKey: .packType) {
      self.packType = value
    }
    if let value = try container.decodeIfPresent([PackLogType].self, forKey: .packLogTypes) {
      self.packLogTypes = value
    }
    if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .hidden) {
      self.hidden = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .packDocumentation) {
      self.packDocumentation = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.displayName, forKey: .displayName)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.icon, forKey: .icon)
    try container.encode(self.categories, forKey: .categories)
    try container.encode(self.packType, forKey: .packType)
    try container.encode(self.packLogTypes, forKey: .packLogTypes)
    try container.encode(self.hidden, forKey: .hidden)
    try container.encode(self.packDocumentation, forKey: .packDocumentation)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Type of feed pack. Feeds Page currently only lists PRODUCT_BASED packs.
  public enum PackType: Codable, Equatable, Sendable {
    /// Unspecified feed pack type.
    case unspecified
    /// Product based feed pack type.
    case productBased
    /// Use case based feed pack type.
    case usecaseBased
    /// Onboarding feed pack type.
    case onboarding
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .productBased: return 1
      case .usecaseBased: return 2
      case .onboarding: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PACK_TYPE_UNSPECIFIED"
      case .productBased: return "PRODUCT_BASED"
      case .usecaseBased: return "USECASE_BASED"
      case .onboarding: return "ONBOARDING"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:PackType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PACK_TYPE_UNSPECIFIED": self = .unspecified
      case "PRODUCT_BASED": self = .productBased
      case "USECASE_BASED": self = .usecaseBased
      case "ONBOARDING": self = .onboarding
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:PackType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .productBased
      case 2: self = .usecaseBased
      case 3: self = .onboarding
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("PACK_TYPE_UNSPECIFIED")
      case .productBased: return try container.encode("PRODUCT_BASED")
      case .usecaseBased: return try container.encode("USECASE_BASED")
      case .onboarding: return try container.encode("ONBOARDING")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.chronicle.v1.FeedPack"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
