// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt
import GoogleType

/// Response message for executing a dashboard query.
public struct ExecuteDashboardQueryResponse: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Result rows that are queried.
  public var results: [ExecuteDashboardQueryResponse.ColumnData] = []

  /// Datasource of the query and results.
  public var dataSources: [DataSource] = []

  /// Optional. Last time the cache was refreshed. This would be used by the UI
  /// to show the last updated time.
  public var lastBackendCacheRefreshedTime: GoogleCloudWkt.Timestamp? = nil

  /// Time window against which query was executed.
  public var timeWindow: GoogleType.Interval? = nil

  /// Runtime errors
  public var queryRuntimeErrors: [QueryRuntimeError] = []

  /// Optional. Language features found in the query.
  public var languageFeatures: [LanguageFeature] = []

  /// Initialize a new instance of `ExecuteDashboardQueryResponse`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ExecuteDashboardQueryResponse().with { $0.results = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// LINT.IfChange(stats_data)
  /// Value of the column based on data type.
  public struct ColumnValue: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    public var metadata: ExecuteDashboardQueryResponse.ColumnValue.ValueMetadata? = nil

    public var value: OneOf_Value? = nil

    /// Initialize a new instance of `ColumnValue`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ColumnValue().with { $0.nullVal = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case nullVal = "nullVal"
      case boolVal = "boolVal"
      case bytesVal = "bytesVal"
      case doubleVal = "doubleVal"
      case int64Val = "int64Val"
      case uint64Val = "uint64Val"
      case stringVal = "stringVal"
      case timestampVal = "timestampVal"
      case dateVal = "dateVal"
      case protoVal = "protoVal"
      case metadata = "metadata"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.metadata = try container.decodeIfPresent(
        ExecuteDashboardQueryResponse.ColumnValue.ValueMetadata.self, forKey: .metadata)

      var value: OneOf_Value? = nil
      let valueCheckAndSet = {
        if value != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'value'"))
        }
        value = $0
      }
      if let nullVal = try container.decodeIfPresent(Swift.Bool.self, forKey: .nullVal) {
        try valueCheckAndSet(.nullVal(nullVal))
      }
      if let boolVal = try container.decodeIfPresent(Swift.Bool.self, forKey: .boolVal) {
        try valueCheckAndSet(.boolVal(boolVal))
      }
      if let bytesVal = try container.decodeIfPresent(Foundation.Data.self, forKey: .bytesVal) {
        try valueCheckAndSet(.bytesVal(bytesVal))
      }
      if let doubleVal = try container.decodeIfPresent(Swift.Double.self, forKey: .doubleVal) {
        try valueCheckAndSet(.doubleVal(doubleVal))
      }
      if let int64Val = try container.decodeIfPresent(Swift.Int64.self, forKey: .int64Val) {
        try valueCheckAndSet(.int64Val(int64Val))
      }
      if let uint64Val = try container.decodeIfPresent(Swift.UInt64.self, forKey: .uint64Val) {
        try valueCheckAndSet(.uint64Val(uint64Val))
      }
      if let stringVal = try container.decodeIfPresent(Swift.String.self, forKey: .stringVal) {
        try valueCheckAndSet(.stringVal(stringVal))
      }
      if let timestampVal = try container.decodeIfPresent(
        GoogleCloudWkt.Timestamp?.self, forKey: .timestampVal)
      {
        try valueCheckAndSet(.timestampVal(timestampVal))
      }
      if let dateVal = try container.decodeIfPresent(GoogleType.Date?.self, forKey: .dateVal) {
        try valueCheckAndSet(.dateVal(dateVal))
      }
      if let protoVal = try container.decodeIfPresent(GoogleCloudWkt.`Any`?.self, forKey: .protoVal)
      {
        try valueCheckAndSet(.protoVal(protoVal))
      }
      self.value = value
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.metadata, forKey: .metadata)

      if let choice = self.value {
        switch choice {
        case .nullVal(let value):
          try container.encode(value, forKey: .nullVal)
        case .boolVal(let value):
          try container.encode(value, forKey: .boolVal)
        case .bytesVal(let value):
          try container.encode(value, forKey: .bytesVal)
        case .doubleVal(let value):
          try container.encode(value, forKey: .doubleVal)
        case .int64Val(let value):
          try container.encode(value, forKey: .int64Val)
        case .uint64Val(let value):
          try container.encode(value, forKey: .uint64Val)
        case .stringVal(let value):
          try container.encode(value, forKey: .stringVal)
        case .timestampVal(let value):
          try container.encode(value, forKey: .timestampVal)
        case .dateVal(let value):
          try container.encode(value, forKey: .dateVal)
        case .protoVal(let value):
          try container.encode(value, forKey: .protoVal)
        }
      }
    }

    public struct ValueMetadata: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// "Auto" generated In-app links.
      public var links: [InAppLink] = []

      public var fieldPaths: [Swift.String] = []

      /// Timestamp value to store the timestamp for the case of the date and
      /// time data type.
      public var timestampVal: GoogleCloudWkt.Timestamp? = nil

      /// Initialize a new instance of `ValueMetadata`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ValueMetadata().with { $0.links = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.chronicle.v1.ExecuteDashboardQueryResponse.ColumnValue.ValueMetadata"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public enum OneOf_Value: Codable, Equatable, Sendable {
      /// True if the value is NULL.
      case nullVal(Swift.Bool)
      /// Boolean value.
      case boolVal(Swift.Bool)
      /// Bytes value.
      case bytesVal(Foundation.Data)
      /// Double value.
      case doubleVal(Swift.Double)
      /// Integer value (signed).
      case int64Val(Swift.Int64)
      /// Un-signed integer value.
      case uint64Val(Swift.UInt64)
      /// String value. Enum values are returned as strings.
      case stringVal(Swift.String)
      /// Timestamp values. Does not handle `interval`.
      indirect case timestampVal(GoogleCloudWkt.Timestamp?)
      /// Date values.
      indirect case dateVal(GoogleType.Date?)
      /// For any proto values that are not any of the above.
      indirect case protoVal(GoogleCloudWkt.`Any`?)
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.chronicle.v1.ExecuteDashboardQueryResponse.ColumnValue"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Singular vs list of values in a column.
  public struct ColumnType: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    public var type: OneOf_Type? = nil

    /// Initialize a new instance of `ColumnType`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ColumnType().with { $0.value = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case value = "value"
      case list = "list"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)

      var type: OneOf_Type? = nil
      let typeCheckAndSet = {
        if type != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'type'"))
        }
        type = $0
      }
      if let value = try container.decodeIfPresent(
        ExecuteDashboardQueryResponse.ColumnValue?.self, forKey: .value)
      {
        try typeCheckAndSet(.value(value))
      }
      if let list = try container.decodeIfPresent(
        ExecuteDashboardQueryResponse.ColumnType.List?.self, forKey: .list)
      {
        try typeCheckAndSet(.list(list))
      }
      self.type = type
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)

      if let choice = self.type {
        switch choice {
        case .value(let value):
          try container.encode(value, forKey: .value)
        case .list(let value):
          try container.encode(value, forKey: .list)
        }
      }
    }

    /// Store list of values in a column.
    public struct List: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      public var values: [ExecuteDashboardQueryResponse.ColumnValue] = []

      /// Initialize a new instance of `List`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = List().with { $0.values = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.chronicle.v1.ExecuteDashboardQueryResponse.ColumnType.List"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public enum OneOf_Type: Codable, Equatable, Sendable {
      /// Single value in a column.
      indirect case value(ExecuteDashboardQueryResponse.ColumnValue?)
      /// List of values in a column e.g. IPs
      indirect case list(ExecuteDashboardQueryResponse.ColumnType.List?)
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.chronicle.v1.ExecuteDashboardQueryResponse.ColumnType"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  public struct ColumnData: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Used to store column names.
    public var column: Swift.String = Swift.String()

    /// To store column data.
    public var values: [ExecuteDashboardQueryResponse.ColumnType] = []

    /// To store column metadata.
    public var metadata: ColumnMetadata? = nil

    /// Initialize a new instance of `ColumnData`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ColumnData().with { $0.column = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.chronicle.v1.ExecuteDashboardQueryResponse.ColumnData"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.chronicle.v1.ExecuteDashboardQueryResponse"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
