// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// Office 365 settings.
public struct Office365Settings: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Input only. Authentication.
  public var authentication: MicrosoftOAuthClientCredentials? = nil

  /// Tenant ID.
  public var tenantId: Swift.String = Swift.String()

  /// Supported office 365 content type.
  public var contentType: Office365Settings.ContentType = Office365Settings.ContentType()

  /// API Hostname.
  public var hostname: Swift.String = Swift.String()

  /// API Auth Endpoint.
  public var authEndpoint: Swift.String = Swift.String()

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `Office365Settings`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Office365Settings().with { $0.authentication = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let authentication = CodingKeys(stringValue: "authentication")
    static let tenantId = CodingKeys(stringValue: "tenantId")
    static let contentType = CodingKeys(stringValue: "contentType")
    static let hostname = CodingKeys(stringValue: "hostname")
    static let authEndpoint = CodingKeys(stringValue: "authEndpoint")

    static let _knownKeys: Set<Swift.String> = [
      "authentication",
      "tenantId",
      "contentType",
      "hostname",
      "authEndpoint",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.authentication = try container.decodeIfPresent(
      MicrosoftOAuthClientCredentials.self, forKey: .authentication)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .tenantId) {
      self.tenantId = value
    }
    if let value = try container.decodeIfPresent(
      Office365Settings.ContentType.self, forKey: .contentType)
    {
      self.contentType = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .hostname) {
      self.hostname = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .authEndpoint) {
      self.authEndpoint = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encodeIfPresent(self.authentication, forKey: .authentication)
    try container.encode(self.tenantId, forKey: .tenantId)
    try container.encode(self.contentType, forKey: .contentType)
    try container.encode(self.hostname, forKey: .hostname)
    try container.encode(self.authEndpoint, forKey: .authEndpoint)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Office 365 supported content types:
  /// https://docs.microsoft.com/en-us/office/office-365-management-api/office-365-management-activity-api-reference#working-with-the-office-365-management-activity-api
  public enum ContentType: Codable, Equatable, Sendable {
    /// Unspecified content type.
    case unspecified
    /// Audit.AzureActiveDirectory.
    case auditAzureActiveDirectory
    /// Audit.Exchange.
    case auditExchange
    /// Audit.SharePoint.
    case auditSharePoint
    /// Audit.General.
    case auditGeneral
    /// DLP.All.
    case dlpAll
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .auditAzureActiveDirectory: return 1
      case .auditExchange: return 2
      case .auditSharePoint: return 3
      case .auditGeneral: return 4
      case .dlpAll: return 5
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "CONTENT_TYPE_UNSPECIFIED"
      case .auditAzureActiveDirectory: return "AUDIT_AZURE_ACTIVE_DIRECTORY"
      case .auditExchange: return "AUDIT_EXCHANGE"
      case .auditSharePoint: return "AUDIT_SHARE_POINT"
      case .auditGeneral: return "AUDIT_GENERAL"
      case .dlpAll: return "DLP_ALL"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ContentType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "CONTENT_TYPE_UNSPECIFIED": self = .unspecified
      case "AUDIT_AZURE_ACTIVE_DIRECTORY": self = .auditAzureActiveDirectory
      case "AUDIT_EXCHANGE": self = .auditExchange
      case "AUDIT_SHARE_POINT": self = .auditSharePoint
      case "AUDIT_GENERAL": self = .auditGeneral
      case "DLP_ALL": self = .dlpAll
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ContentType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .auditAzureActiveDirectory
      case 2: self = .auditExchange
      case 3: self = .auditSharePoint
      case 4: self = .auditGeneral
      case 5: self = .dlpAll
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("CONTENT_TYPE_UNSPECIFIED")
      case .auditAzureActiveDirectory: return try container.encode("AUDIT_AZURE_ACTIVE_DIRECTORY")
      case .auditExchange: return try container.encode("AUDIT_EXCHANGE")
      case .auditSharePoint: return try container.encode("AUDIT_SHARE_POINT")
      case .auditGeneral: return try container.encode("AUDIT_GENERAL")
      case .dlpAll: return try container.encode("DLP_ALL")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.chronicle.v1.Office365Settings"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
