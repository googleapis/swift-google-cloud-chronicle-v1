// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// DataTableColumnInfo represents the column metadata of the
/// datatable. The column_index represents the ordering of the
/// values in DataTableRow.
public struct DataTableColumnInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Required. Column Index. 0,1,2...
  public var columnIndex: Swift.Int32 = Swift.Int32()

  /// Required. Original column name of the Data Table (present in the CSV header
  /// in case of creation of data tables using file uploads). It must satisfy the
  /// following requirements:
  /// - Starts with letter.
  /// - Contains only letters, numbers and underscore.
  /// - Must be unique and has length < 256.
  public var originalColumn: Swift.String = Swift.String()

  /// Optional. Whether to include this column in the calculation of the row ID.
  /// If no columns have key_column = true, all columns will be included in the
  /// calculation of the row ID.
  public var keyColumn: Swift.Bool = Swift.Bool()

  /// Optional. Whether the column is a repeated values column.
  public var repeatedValues: Swift.Bool = Swift.Bool()

  public var pathOrType: OneOf_PathOrType? = nil

  /// Initialize a new instance of `DataTableColumnInfo`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = DataTableColumnInfo().with { $0.mappedColumnPath = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case mappedColumnPath = "mappedColumnPath"
    case columnType = "columnType"
    case columnIndex = "columnIndex"
    case originalColumn = "originalColumn"
    case keyColumn = "keyColumn"
    case repeatedValues = "repeatedValues"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.columnIndex = try container.decode(Swift.Int32.self, forKey: .columnIndex)
    self.originalColumn = try container.decode(Swift.String.self, forKey: .originalColumn)
    self.keyColumn = try container.decode(Swift.Bool.self, forKey: .keyColumn)
    self.repeatedValues = try container.decode(Swift.Bool.self, forKey: .repeatedValues)

    var pathOrType: OneOf_PathOrType? = nil
    let pathOrTypeCheckAndSet = {
      if pathOrType != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'pathOrType'"))
      }
      pathOrType = $0
    }
    if let mappedColumnPath = try container.decodeIfPresent(
      Swift.String.self, forKey: .mappedColumnPath)
    {
      try pathOrTypeCheckAndSet(.mappedColumnPath(mappedColumnPath))
    }
    if let columnType = try container.decodeIfPresent(
      DataTableColumnInfo.DataTableColumnType.self, forKey: .columnType)
    {
      try pathOrTypeCheckAndSet(.columnType(columnType))
    }
    self.pathOrType = pathOrType
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.columnIndex, forKey: .columnIndex)
    try container.encode(self.originalColumn, forKey: .originalColumn)
    try container.encode(self.keyColumn, forKey: .keyColumn)
    try container.encode(self.repeatedValues, forKey: .repeatedValues)

    if let choice = self.pathOrType {
      switch choice {
      case .mappedColumnPath(let value):
        try container.encode(value, forKey: .mappedColumnPath)
      case .columnType(let value):
        try container.encode(value, forKey: .columnType)
      }
    }
  }

  /// DataTableColumnType denotes the type of the column to be referenced in the
  /// rule.
  public enum DataTableColumnType: Codable, Equatable, Sendable {
    /// The default Data Table Column Type.
    case unspecified
    /// Denotes the type of the column as STRING.
    case string
    /// Denotes the type of the column as REGEX.
    case regex
    /// Denotes the type of the column as CIDR.
    case cidr
    /// Denotes the type of the column as NUMBER (includes int and float).
    case number
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .string: return 1
      case .regex: return 2
      case .cidr: return 3
      case .number: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "DATA_TABLE_COLUMN_TYPE_UNSPECIFIED"
      case .string: return "STRING"
      case .regex: return "REGEX"
      case .cidr: return "CIDR"
      case .number: return "NUMBER"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:DataTableColumnType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "DATA_TABLE_COLUMN_TYPE_UNSPECIFIED": self = .unspecified
      case "STRING": self = .string
      case "REGEX": self = .regex
      case "CIDR": self = .cidr
      case "NUMBER": self = .number
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:DataTableColumnType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .string
      case 2: self = .regex
      case 3: self = .cidr
      case 4: self = .number
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .string: return try container.encode(1)
      case .regex: return try container.encode(2)
      case .cidr: return try container.encode(3)
      case .number: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public enum OneOf_PathOrType: Codable, Equatable, Sendable {
    /// Entity proto field path that the column is mapped to
    case mappedColumnPath(Swift.String)
    /// Column type can be STRING, CIDR (Ex- 10.1.1.0/24), REGEX
    case columnType(DataTableColumnInfo.DataTableColumnType)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.chronicle.v1.DataTableColumnInfo"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
